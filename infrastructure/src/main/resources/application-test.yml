spring:
    datasource:
        url: jdbc:h2:mem:pagbank_challenge_test;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
        username: root
        password: root
        driver-class-name: org.h2.Driver
    h2:
        console:
            enabled: true
            path: /h2